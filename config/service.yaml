apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: jmeter-service
  namespace: keptn
spec:
  runLatest:
    configuration:
      revisionTemplate:
        metadata:
          annotations:
            autoscaling.knative.dev/minScale: "1"
        spec:
          container:
            image: keptn/jmeter-service:latest
            imagePullPolicy: Always
---
apiVersion: eventing.knative.dev/v1alpha1
kind: Subscription
metadata:
  name: jmeter-deployment-finished-changed
  namespace: keptn
spec:
  channel:
    apiVersion: eventing.knative.dev/v1alpha1
    kind: Channel
    name: deployment-finished
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1alpha1
      kind: Service
      name: jmeter-service
